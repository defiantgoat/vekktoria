import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  app: {
    width: '100%',
    height: '100%',
    overflow: 'hidden',
    position: 'absolute',
    backgroundColor: '#eee',
    display: 'flex',
    flexDirection: 'row'
  },
});

export default useStyles;
